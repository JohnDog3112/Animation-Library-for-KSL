<TIMESTAMP as number>: <COMMAND>
COMMANDS:
    SPAWN <BASE ENTITY ID> AS <ENTITY ID> (AT <STATION ID>)
    QUEUE <QUEUE ID> JOIN <ENTITY ID>
    QUEUE <QUEUE ID> LEAVE <ENTITY ID>
    RESOURCE SET STATE <NEW STATE>
    MOVE <ENTITY ID> TO <STATION ID> (AS <INSTANT|EASE_IN|EASE_OUT|EASE_IN_OUT>)
    VARIABLE <VARIABLE ID> SET <VALUE>
    GRAPH
